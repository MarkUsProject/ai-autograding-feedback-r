% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/template_utils.R
\name{get_next_heading_title}
\alias{get_next_heading_title}
\title{Given a flattened TOC (table of contents) list and a specific heading title,
this function returns the title of the next heading that is at the same or higher
level in the hierarchy (i.e., not a child/subheading).}
\usage{
get_next_heading_title(toc, heading)
}
\arguments{
\item{toc}{A flattened list of TOC entries, as returned by \code{flatten_toc()}.
Each TOC entry should have at least \code{title} and \code{level} fields.}

\item{heading}{A character string representing the heading title to search for.}
}
\value{
The title (character string) of the next heading at the same or higher level,
or \code{NULL} if there is no such heading.
}
\description{
Given a flattened TOC (table of contents) list and a specific heading title,
this function returns the title of the next heading that is at the same or higher
level in the hierarchy (i.e., not a child/subheading).
}
